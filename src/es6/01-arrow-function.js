function square(num) {
  return num * num;
}

// Arrow function
const squareArrow = (num) => {
    return num * num;
    }

// Arrow function expression syntax
const squareArrow2 = (num) => num * num;  // implicit return